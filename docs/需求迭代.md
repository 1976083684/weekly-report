# 需求迭代记录

---

## 2026-05-08

### 新增

| 文件 | 说明 |
|------|------|
| `src/app/(dashboard)/settings/account/page.tsx` | 设置 → 我的信息子页面（邮箱/昵称/OAuth绑定/切换账号/退出登录） |
| `src/app/(dashboard)/settings/password/page.tsx` | 设置 → 修改密码子页面 |
| `src/app/(dashboard)/settings/backup/page.tsx` | 设置 → 备份配置子页面（Gitee/GitHub仓库配置） |
| `src/app/(dashboard)/settings/import-export/page.tsx` | 设置 → 导入导出子页面（JSON数据导入导出） |
| `src/app/(dashboard)/settings/models/page.tsx` | 设置 → 模型配置页面（预设模型/手动添加/测试连接/启用切换/API Key管理/配置JSON查看） |
| `src/app/api/tags/batch-delete/route.ts` | 标签批量删除API（POST，验证归属后批量删除DiaryTag+Tag） |
| `src/app/api/settings/import/route.ts` | 设置数据导入API |
| `src/app/api/settings/models/route.ts` | 模型配置 GET/POST API（首次自动创建预设，列出/添加模型） |
| `src/app/api/settings/models/[id]/route.ts` | 模型配置 PUT/DELETE API（更新模型/修改Key/启用停用/删除） |
| `src/app/api/settings/models/[id]/test/route.ts` | 模型连接测试API（发送"你好"验证API Key可用性） |
| `src/app/api/backup/config/decrypt-token/route.ts` | 备份Token解密查看API |
| `src/app/api/backup/test-connection/route.ts` | 备份仓库连接测试API |
| `src/app/api/ai/optimize/route.ts` | AI优化API（POST，携带日记/日报/周报专用Agent Skills系统提示词，调用启用的模型API） |
| `src/components/ai/AiOptimizeButton.tsx` | AI优化按钮组件（加载态/结果预览/原文对比/采纳放弃） |
| `src/lib/auth.config.ts` | Auth Edge兼容配置（JWT策略+OAuth+回调，不含Prisma依赖，供中间件使用） |
| `src/lib/preset-models.ts` | 预设模型定义（DeepSeek/智谱AI的供应商/模型名/官网/BaseURL/模型映射/配置JSON） |

### 变更

| 文件 | 说明 |
|------|------|
| `prisma/schema.prisma` | 新增 AIModel 表（provider/modelName/apiKey/baseUrl/website/notes/haikuModel/sonnetModel/opusModel/configJson/isActive），User 表新增 aiModels 关联 |
| `src/app/(dashboard)/dashboard/page.tsx` | 仪表盘首页去除"写日记"按钮；新增 toLocalDateStr 本地日期工具函数；活跃度热力图改为滚动365天窗口；isFuture 改用字符串比较 |
| `src/app/(dashboard)/diary/page.tsx` | 日记列表页 header 新增"写日记"文字按钮（参考日报页面，含 Plus 图标） |
| `src/app/(dashboard)/tags/page.tsx` | 标签管理页重构：顶部新增创建标签输入框+添加按钮；新增全选/多选框+批量删除按钮；每行标签显示关联日记数 |
| `src/app/(dashboard)/settings/page.tsx` | 设置主页重构：提取账号信息到子页面；改为导航卡片（我的信息/修改密码/备份配置/模型配置/导入导出）；保留删除账号区域 |
| `src/app/(dashboard)/weekly/page.tsx` | 周报页面：周报按钮改为始终设置 showWeekly=true（不切换）；修复切换日报→周报时无选中日期的崩溃 |
| `src/app/api/dashboard/stats/route.ts` | 仪表盘统计API：新增 toLocalDateStr；滚动年逻辑（today-365→today）；跨年月度标签动态计算；返回 isCurrentYear/totalDiariesInRange/monthLabels |
| `src/app/api/diary/route.ts` | 日记查询API增强 |
| `src/app/api/backup/config/route.ts` | 备份配置API增强（解密Token/测试连接支持） |
| `src/app/api/settings/export/route.ts` | 设置导出API增强 |
| `src/components/backup/BackupConfigForm.tsx` | 备份配置表单增强（新增解密Token/测试连接UI） |
| `src/components/diary/DiaryForm.tsx` | 日记表单集成 AiOptimizeButton（AI优化日记） |
| `src/components/weekly/WeeklyForm.tsx` | 周报表单集成 AiOptimizeButton（日报编辑器+周报编辑器两处）；周报按钮行为调整 |
| `src/lib/auth.ts` | Auth配置拆分：提取Edge兼容配置到 auth.config.ts；本文件保留 PrismaAdapter + Credentials 的完整配置 |
| `src/lib/backup.ts` | 备份核心逻辑：新增 hardBreaks() 函数将单 \n 转为 Markdown 硬换行（双空格+\n）；formatDiary/formatDailyReport/formatWeekly 使用 \n\n 分节 + hardBreaks 处理内容 |
| `src/lib/diary.ts` | 日记库增强 |
| `src/lib/gitee.ts` | Gitee API库增强 |
| `src/lib/github.ts` | GitHub API库增强 |
| `src/middleware.ts` | 中间件改用 `NextAuth(authConfig)` 而非导入 `@/lib/auth`，避免 Edge Runtime 加载 Prisma |

### 优化

| 文件 | 说明 |
|------|------|
| `src/app/(dashboard)/dashboard/page.tsx` | 活跃度热力图滚动365天显示（最新一年展示到当前日期）；月度分布鼠标悬停显示当月具体数量；toLocalDateStr 替代 toISOString 解决 UTC+8 时区日期偏差 |
| `src/lib/backup.ts` | 备份到 Gitee/GitHub 的内容：单 \n 转 Markdown 硬换行 `  \n` 确保行内换行正确渲染 |
| `src/components/ai/AiOptimizeButton.tsx` | 错误提示从笼统"网络请求失败"改为显示 HTTP 状态码 + 具体原因 |
| `src/app/api/ai/optimize/route.ts` | 首次调用自动创建预设模型（DeepSeek默认启用），无需先访问设置页面 |

### 修复Bug

| 文件 | 问题描述 |
|------|----------|
| `src/middleware.ts` | **Edge Runtime 500错误** — 中间件导入 `@/lib/auth` 导致 PrismaAdapter + @prisma/client 在 Edge 环境无法加载，所有请求返回500。修复：拆分为 `auth.config.ts`（Edge安全）+ `auth.ts`（Node.js），中间件使用 NextAuth(authConfig) |
| `src/app/api/ai/optimize/route.ts` | **AI优化按钮报500** — Prisma Client未重新生成导致 `aIModel` 属性 undefined。清除 node_modules/.prisma/client 后重新生成 |
| `src/components/ai/AiOptimizeButton.tsx` | **AI优化按钮报"网络请求失败"** — 旧 dev server 进程未包含 `/api/ai/optimize` 路由，fetch 返回 HTML 导致 JSON 解析失败 |
| `src/app/(dashboard)/dashboard/page.tsx` | **活跃度日期偏差** — `toISOString().split("T")[0]` 在 UTC+8 时区可能差一天（如周一显示为周日日期）。使用 getFullYear/getMonth/getDate 的本地日期替代 |
| `src/app/(dashboard)/dashboard/page.tsx` | **isFuture 判断不稳定** — Date 对象比较 `day.date > new Date()` 含时间分量。改为字符串日期比较 `dateStr > toLocalDateStr(new Date())` |

---

### 涉及文件统计

- **新增文件**: 16 个
- **修改文件**: 21 个
- **代码变更**: +946 行 / -399 行

---

## 2026-05-08（第二批）

### 新增

| 文件 | 说明 |
|------|------|
| `src/lib/utils.ts` | 新增 `toLocalDateStr`、`todayStr`、`parseLocalDate` 三个本地时区日期工具函数 |
| `src/app/api/settings/route.ts` | 新增 PUT 接口，支持更新手机号（校验唯一性） |

### 变更

| 文件 | 说明 |
|------|------|
| `prisma/schema.prisma` | User 表新增 `username String? @unique` 和 `phone String? @unique` 字段 |
| `src/app/(auth)/login/page.tsx` | 登录输入框改为「邮箱 / 手机号」统一输入，`signIn` 参数从 `email` 改为 `identifier` |
| `src/app/(auth)/register/page.tsx` | 注册表单去掉用户名输入框，新增手机号选填输入框（提示"绑定后可用手机号登录"） |
| `src/app/api/auth/register/route.ts` | 注册接口去掉 `username` 必填；`phone` 改为可选（格式校验）；分别校验邮箱和手机号唯一性，返回具体冲突提示 |
| `src/lib/auth.ts` | Credentials Provider 的 `email` 字段改为 `identifier`；`authorize` 查询改为 `OR: [email, phone]` 支持邮箱/手机号登录 |
| `src/app/(dashboard)/settings/account/page.tsx` | 个人详情页新增手机号显示和内联编辑绑定（铅笔图标→输入框→保存/取消）；去掉用户名显示行 |
| `src/app/api/settings/route.ts` | GET 接口返回值新增 `phone` 字段 |
| `src/app/(dashboard)/settings/models/page.tsx` | 余额显示改为上下两行对称布局（上行：时间+刷新按钮，下行：余额数值绿色字体）；开启用量查询即显示区块，无值时占位 `--`，失败时显示「检查配置」；`refreshBalance` 新增默认提取器兜底 |
| `src/app/api/settings/models/route.ts` | `parseUsageBalance` 不再要求 `enabled` 才返回数据；返回结构新增 `enabled`、`error` 字段 |
| `src/app/api/settings/models/[id]/usage-test/route.ts` | 新增内置 `builtInExtractor`，按优先级智能解析 `balance_infos[0].total_balance`、`total_balance`、`balance` 三种格式；自定义提取器仅在取到非空值时覆盖；测试失败保存 `lastError`，成功清除 |
| `src/app/(dashboard)/settings/models/[id]/page.tsx` | `DEFAULT_USAGE_BLOCK` 提取器更新：`info.total_balance \|\| response.total_balance \|\| response.balance`，单位从 `info.currency` 读取；`handleUsageTest` 测试成功后自动 `setUsageEnabled(true)` 并同步本地 `rawConfigJson` |
| `src/app/api/ai/optimize/route.ts` | 移除硬编码 DeepSeek API Key（`PRESET_API_KEYS`）；预设模型创建时 `isActive` 改为 `false`，`apiKey` 改为空字符串 |
| `src/app/api/dashboard/stats/route.ts` | 日期处理改用 `toLocalDateStr` + `parseLocalDate` 替代 `toISOString` + `new Date(str)`，避免 UTC+8 时区日期偏差 |
| `src/app/api/settings/export/route.ts` | 导出数据中的日期改用 `toLocalDateStr`，避免跨时区日期偏移 |
| `src/app/(dashboard)/diary/[id]/edit/page.tsx` | 日记编辑页日期改用 `toLocalDateStr` |
| `src/app/(dashboard)/weekly/[id]/edit/page.tsx` | 周报编辑页日期改用 `toLocalDateStr` |
| `src/components/diary/DiaryForm.tsx` | 默认日期改用 `todayStr()`，`defaultTitle` 日期解析改用 `parseLocalDate` |
| `src/components/weekly/WeeklyForm.tsx` | 当前日期改用 `todayStr()`，周日期计算改用 `toLocalDateStr` |
| `src/lib/backup.ts` | 备份文件日期改用 `toLocalDateStr`；清理冗余注释 |
| `src/lib/diary.ts` | 日期解析统一改用 `parseLocalDate`（`new Date(str)` 在 UTC+8 会偏移一天） |
| `src/lib/weekly.ts` | 日期解析统一改用 `parseLocalDate` |

### 优化

| 文件 | 说明 |
|------|------|
| `src/app/(dashboard)/settings/models/page.tsx` | 余额数值使用绿色字体 (`text-success`)；时间/刷新按钮与余额行上下交换 |
| `src/app/api/settings/models/[id]/usage-test/route.ts` | API 端内置智能余额解析器，不再依赖前端旧版提取器，即使数据库存有旧版错误提取器也能正确解析余额 |
| `src/lib/utils.ts` | 统一日期工具函数，全局替换 12 处 `toISOString().slice(0,10)` 和 `new Date(str)` 为本地时区安全版本 |

### 修复Bug

| 文件 | 问题描述 |
|------|----------|
| `src/app/api/settings/models/[id]/usage-test/route.ts` | **余额取值不对** — 旧版提取器 `response.balance` 无法解析 `balance_infos[0].total_balance` 结构（智谱AI等），返回空值。修复：API 内置智能解析器，自定义提取器取不到值时自动回退 |
| `src/lib/diary.ts` | **日期偏移** — `new Date("2026-05-08")` 在 UTC+8 时区解析为前一天 16:00 (UTC)，导致日期存储错误。修复：改用 `parseLocalDate` 在本地午间创建 Date |
| `src/lib/weekly.ts` | 同上，周报日期解析改用 `parseLocalDate` |
| `src/lib/backup.ts` | **备份文件名日期偏移** — `toISOString().slice(0,10)` 在 UTC+8 可能导致跨天文件名错误，改用 `toLocalDateStr` |

### 涉及文件统计

- **新增文件**: 0 个
- **修改文件**: 22 个
- **代码变更**: +455 行 / -266 行

---

## 2026-05-11

### 新增

| 文件 | 说明 |
|------|------|
| `src/components/editor/VoiceInput.tsx` | 语音输入按钮组件：点击开始/停止录音，红色脉冲动画指示录音状态，利用 ref 保持最新回调实现实时追加文字到输入框 |
| `src/hooks/useVoiceRecognition.ts` | 语音识别 Hook：封装 Web Speech API（zh-CN），`continuous:false` 模式 + `onend` 自动重建实例保持持续监听；`getUserMedia` 预请求麦克风权限；支持 Chrome/Edge |

### 变更

| 文件 | 说明 |
|------|------|
| `prisma/schema.prisma` | User 表 `email` 字段从 `String @unique` 改为 `String? @unique`（邮箱选填）；级联删除链完整确认（所有关联模型均 `onDelete: Cascade`） |
| `src/app/(auth)/register/page.tsx` | 注册表单字段顺序调整：手机号上移并设为必填（`required`），邮箱下移加"（选填）"标注；提交数据顺序从 `email,password,name,phone` 改为 `phone,password,name,email` |
| `src/app/(auth)/login/page.tsx` | 新增 OAuth 回调错误处理：`useSearchParams` 读取 `?error=` 参数，映射 10 种错误码到中文提示；新增 `useEffect` 监听 URL 参数变化 |
| `src/app/api/auth/register/route.ts` | 注册校验重构：`phone` 从可选改为必填（删除 `.optional().or(z.literal(""))`）；`email` 改为可选；唯一性检查先查手机号再查邮箱；昵称回退值从 `email前缀` 改为 `phone` |
| `src/components/editor/MarkdownEditor.tsx` | 工具栏新增 `<VoiceInput>` 组件（编辑模式下可见）；`handleTranscript` 回调负责追加语音文字到 textarea 末尾并自动聚焦滚动到底部 |
| `src/lib/auth.ts` | GiteeProvider 优化：`authorization` 从字符串改为对象 `{url, params: {scope: "user_info"}}`；`userinfo` 从字符串改为对象含自定义 `request` 方法（Gitee API 要求 `access_token` 作为 URL 查询参数而非 Bearer header） |
| `src/lib/auth.config.ts` | 同上，GiteeProvider 同步优化确保 Edge 兼容配置一致 |

### 优化

| 文件 | 说明 |
|------|------|
| `src/app/(auth)/login/page.tsx` | OAuth 登录失败时不再显示空白页面，自动展示对应中文错误原因（如"该邮箱已绑定其他登录方式"、"第三方登录回调失败"等） |
| `src/hooks/useVoiceRecognition.ts` | `continuous:false` 比 `continuous:true` 更稳定；`onend` 自动重建实例避免 Chrome 连续模式卡死；`getUserMedia` 预请求避免静默失败 |
| `src/components/editor/VoiceInput.tsx` | 采用 `onTranscriptRef` 模式实时写入，避免 `useCallback([])` 闭包过期导致文字丢失；移除内部累积逻辑，每句话识别完成立即追加 |

### 修复Bug

| 文件 | 问题描述 |
|------|----------|
| `src/hooks/useVoiceRecognition.ts` | **语音录入无声（第一版）** — `continuous:true` + 10秒沉默定时器调 `stop()` 后不重启，导致监听永久中断。修复：去掉沉默定时器，改用 `continuous:false` + `onend` 自动新建实例重启 |
| `src/hooks/useVoiceRecognition.ts` | **语音录入无声（第二版）** — 未预请求麦克风权限，Chrome Speech API 无权限时静默失败不触发 `onerror`。修复：`start()` 开头调 `getUserMedia({audio:true})` |
| `src/components/editor/VoiceInput.tsx` | **识别但文字不入框** — 文本累积在 `accumulatedRef` 中，仅点击停止才提交一次，用户期望实时看到文字。修复：去掉累积逻辑，每句话 `onResult` 立即通过 `onTranscriptRef` 追加到输入框 |
| `src/app/api/auth/register/route.ts` | **注册失败（不填邮箱）** — Prisma Client 未重新生成（dev server 运行中文件锁定），仍用旧 schema（email 必填）。修复：停止 dev server → `prisma generate` → 重启 |

### 涉及文件统计

- **新增文件**: 3 个
- **修改文件**: 8 个
- **代码变更**: +310 行 / -120 行

---

## 2026-05-12

### 新增

| 文件 | 说明 |
|------|------|
| `src/app/api/backup/import/route.ts` | 备份逆向导入 API（POST）：支持从 GitHub / Gitee 备份仓库递归遍历目录，解析 Markdown 文件，去重导入日记/日报/周报到本地数据库 |
| `src/lib/import-parser.ts` | 备份文件解析器：支持 `parseDiaryMarkdown`（含心情映射）、`parseDailyReportMarkdown`、`parseWeeklyMarkdown`、`parseWeeklyPath`（从文件名提取日期范围） |
| `src/app/(dashboard)/settings/import-export/page.tsx` | 新增 `GitImportSection` 通用组件，导入导出页新增「从 GitHub 导入」和「从 Gitee 导入」两个独立区块 |

### 变更

| 文件 | 说明 |
|------|------|
| `src/app/layout.tsx` | 项目名称从「日记周报」更名为「AI随记」；描述改为「AI驱动的个人日记与周报管理系统」 |
| `src/app/(auth)/login/page.tsx` | 登录页标题更名为「AI随记」；副标题改为「AI驱动，智慧记录每一天」 |
| `src/app/(auth)/register/page.tsx` | 注册页标题更名为「AI随记」；副标题改为「AI驱动，智慧记录每一天」 |
| `src/components/layout/Sidebar.tsx` | 侧边栏品牌名更名为「AI随记」；标语改为「AI驱动，智慧记录」 |
| `README.md` | 项目名称和描述更新；新增逆向导入等功能说明 |
| `docs/PRD.md` | 标题从「日记周报」更名为「AI随记」 |
| `src/app/(dashboard)/weekly/page.tsx` | 周报列表排序三级排序：按周分组→每周周报置顶→日报日期倒序；`getMonday` 加 `isNaN` 校验 |
| `src/components/weekly/WeeklyForm.tsx` | 写周报页默认选中当天日报（`showWeekly` 初始值 `false`）；移除加载已有周报时的强制覆盖 |
| `src/app/(dashboard)/settings/account/page.tsx` | 用户信息页新增用户名编辑功能：内联修改 → 保存/取消 |
| `src/app/api/settings/route.ts` | PUT 接口新增 `name` 字段支持（非空校验 + 长度限制 50 字符） |
| `src/app/(dashboard)/settings/import-export/page.tsx` | 重构为 GitHub/Gitee 双通道逆向导入，提取 `GitImportSection` 通用组件 |

### 优化

| 文件 | 说明 |
|------|------|
| `src/app/(dashboard)/settings/import-export/page.tsx` | `GitImportSection` 通用组件消除 GitHub/Gitee 重复代码；单次 API 加载所有配置 |
| `src/app/api/backup/import/route.ts` | provider 无关设计：`apiBase`/`fetchHeaders`/`contentsUrl` 自动切换 GitHub/Gitee API 差异 |

### 修复Bug

| 文件 | 问题描述 |
|------|----------|
| `src/components/weekly/WeeklyForm.tsx` | **首次进入写周报显示周报而非当天日报** — `showWeekly` 默认 `true` + `useEffect` 中 `setShowWeekly(true)` 双重覆盖。修复：初始值 `false` 且移除强制覆盖 |
| `src/app/(dashboard)/weekly/page.tsx` | **周报列表崩溃 RangeError** — `getMonday` 对周报完整 ISO 字符串 `"2026-05-11T00:00:00.000Z"` 拼接 `"T12:00:00"` 产生非法日期，fetchData 崩掉导致列表内容加载不出来。修复：`.slice(0,10)` 截取日期前缀 + `isNaN` 校验兜底 |

### 涉及文件统计

- **新增文件**: 3 个
- **修改文件**: 12 个
- **代码变更**: +380 行 / -35 行

